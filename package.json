{
  "name": "oracle-truth",
  "version": "0.1.0",
  "private": true,
  "description": "Pre-trade safety gate for perps and prediction markets on Solana",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "clients/ts"
  ],
  "scripts": {
    "build": "npm run build --workspace @oracle-truth/core && npm run build --workspace @oracle-truth/api && npm run build --workspace @oracle-truth/client",
    "test": "npm run test --workspace @oracle-truth/core",
    "dev": "npm run dev --workspace @oracle-truth/api",
    "start": "npm run start --workspace @oracle-truth/api"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@oracle-truth/core": "*"
  },
  "devDependencies": {
    "@vercel/node": "^5.5.0"
  }
}
